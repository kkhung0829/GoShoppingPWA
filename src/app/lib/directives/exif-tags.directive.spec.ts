import { ExifTagsDirective } from './exif-tags.directive';

describe('ExifTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new ExifTagsDirective();
    expect(directive).toBeTruthy();
  });
});
